#!/usr/bin/env node

console.log('TODO: CLI incomplete');
process.exit(1);

// return todoStream
//   .pipe(todoParse())
//   .pipe(filterComplete())
//   .pipe(filterByDueDate(moment()))
//   .pipe(sendToProwl('apiKey'));
